language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-drupal' ]; then cd .. && eval "mv $currentfolder generator-angular-drupal" && cd generator-angular-drupal; fi
